// Print the ASCII value od the declared character

#include <stdio.h>

int main(void);

int main(void) {
    char c = 'a';
    printf("The character \'%c\' has the ASCII value %d\n" ,c,c);
    return 0;   /* Return success to the operating system. */
}

